name: flashride_app
description: FlashRide – mapa + autocomplete sin conflictos
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.6.0
  http: ^0.13.6
  geolocator: ^11.1.0
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.2
  logging: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
